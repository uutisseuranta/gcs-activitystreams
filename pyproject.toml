[tool.ruff]
# Kohdepolku ja tiedostot
src = ["src"]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
# Valitut sääntöryhmät tyylille ja tietoturvalle
select = [
    "E",   # pycodestyle (virheet)
    "W",   # pycodestyle (varoitukset)
    "F",   # pyflakes (perusvirheet)
    "I",   # isort (importtien järjestys)
    "B",   # flake8-bugbear (yleiset koodivirheet/suunnitteluongelmat)
    "S",   # flake8-bandit (tietoturvatarkistukset / SAST)
]

ignore = []

[tool.ruff.lint.per-file-ignores]
# Testitiedostoissa sallitaan bandit-tietoturvaohitukset (esim. assertit)
"**/test_*.py" = ["S101"]
"unit-test.sh" = ["S101"]
